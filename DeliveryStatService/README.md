# Delivery statistics service

Service calculating statistics for dashboard.

## Build and run
```yaml
../gradlew :DeliveryStatService:clean :DeliveryStatService:build

java -jar build/libs/DeliveryStatService-1.0-SNAPSHOT-all.jar
```
